import MapaEditor from "./MapaEditor";

/** Rota `/mapa`: tela cheia do editor de mapa (mesmo componente usado na sub-aba "Mapa" da Batalha). */
export default function MapaScreen() {
  return <MapaEditor />;
}
